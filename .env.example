# Copia questo file in ".env" (opzionale: i valori di default funzionano già in locale).

# Porta su cui ascolta uvicorn in sviluppo.
PORT=8001

# Percorso del file SQLite. Default: cicciotodo.db nella cartella del progetto.
DATABASE_URL=sqlite:///./cicciotodo.db
